  <%= fields_for :problem_users, ProblemUser.new  do |problem_form| %>
    <%= problem_form.label :user_id, class: 'form-control' %>
    <%= problem_form.select :user_id, 
      options_for_select( @all_users_mapped), 
      {}, 
      { 
        class: 'problem_user_partial form-control', 
        name: "problem[problem_users_attributes][][user_id]"
       } 
    %>
  <% end %>