<%= form_for @tag,remote: true, html: { class: 'new-tag-form' } do |f| %>
  <%= f.label "Add new tag" %>
  <%= f.text_field :name, placeholder: 'Name of tag', class: 'form-control' %>
  <%= f.submit 'Save', style: 'width:100px;', class: 'btn btn-primary mt-2' %>
<% end %>